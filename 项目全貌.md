# LifeOps Agent V3.0 — 项目全貌

> 一份文档看懂整个项目的结构、功能模块和技术架构。

---

## 一、项目是什么

**LifeOps Agent** 是一个基于 LangGraph 的多 Agent 个人生活管家。核心能力是把**微信账单分析**和**百度地图出行推荐**串起来——你问一句"我这周花了多少钱，晚上推荐个便宜的餐厅"，它会先查账单算出预算，再结合地图和本地美食知识库推荐餐厅。

技术路线：Python 后端（LangGraph 多 Agent）+ uni-app 前端（Vue 3），通过 WebSocket 流式通信。

---

## 二、项目目录总览

```
正式项目地/
├── Allthing/                        # ← Python 后端（核心）
│   ├── main.py                      # CLI 入口（控制台对话）
│   ├── server.py                    # FastAPI + WebSocket 服务端
│   ├── requirements.txt             # Python 依赖
│   │
│   ├── config/                      # 配置层
│   │   ├── config.yml               #   统一配置（模型/路径/邮箱/规则）
│   │   ├── config_loader.py         #   配置加载 + LLM 工厂
│   │   └── user_config.yml          #   用户个性化覆盖
│   │
│   ├── graph/                       # LangGraph 状态机（核心编排）
│   │   ├── state.py                 #   AgentState 定义 + 初始状态
│   │   ├── graph_builder.py         #   主 Agent 图：main_agent ⇄ tools
│   │   ├── bill_node.py             #   Bill Agent 子图（沙箱 ReAct）
│   │   ├── travel_node.py           #   Travel Agent 子图（沙箱 ReAct）
│   │   ├── cross_agent.py           #   主 Agent 调用子 Agent 的工具
│   │   └── tool_tracer.py           #   工具调用全链路追踪
│   │
│   ├── routing/                     # 意图识别 + 主 Agent Prompt
│   │   └── task_decomposer.py       #   正则聚光灯 + MAIN_AGENT_PROMPT_V3
│   │
│   ├── prompts/                     # 4 层动态 Prompt 系统
│   │   ├── assembler.py             #   运行时按需拼装（核心入口）
│   │   ├── version.py               #   Prompt 版本追踪
│   │   ├── base/                    #   L1 基础层（角色 + 工具清单）
│   │   ├── decision/                #   L2 决策层（流程 + 停止规则）
│   │   ├── runtime/                 #   L3 运行时层（降级/预算感知/跨Agent）
│   │   └── failure/                 #   L4 失败层（逐工具重试策略）
│   │
│   ├── tools/                       # 工具生态（5 大模块）
│   │   ├── bill/                    #   账单工具（邮件下载/解析/查询/图表）
│   │   ├── maps/                    #   地图工具（MCP + @tool 双通道）
│   │   ├── knowledge/               #   RAG 知识库（ChromaDB）
│   │   ├── savings/                 #   省钱目标管理
│   │   ├── time/                    #   当前时间
│   │   └── common/                  #   安全计算器
│   │
│   ├── guardrails/                  # 安全护栏
│   │   └── critics.py               #   ToolCallCritic + OutputCritic
│   │
│   ├── llm/                         # LLM 管理
│   │   └── llm_registry.py          #   LLM 注册中心 + 用量追踪
│   │
│   ├── contracts/                   # 数据契约
│   │   └── data_contracts.py         #   FinancialContext 等结构化类型
│   │
│   ├── monitoring/                  # 可观测性
│   │   ├── logger.py                #   统一日志
│   │   ├── rag_logger.py            #   RAG 监控 + JSONL 埋点
│   │   ├── dashboard.py             #   控制台仪表盘
│   │   ├── test_runner.py           #   RAG 批量评测
│   │   └── langsmith_setup.py       #   LangSmith 链路追踪
│   │
│   ├── knowledge_base/              # RAG 原始素材
│   │   ├── food/                    #   美食知识（中山餐厅 90+ 条）
│   │   └── general/                 #   通用知识（中山旅游手册）
│   │
│   ├── data/                        # 运行时数据
│   │   ├── bills/                   #   按月分片 CSV（202603.csv ...）
│   │   ├── savings/                 #   省钱目标 JSON
│   │   ├── checkpoints/             #   LangGraph SQLite checkpoint
│   │   └── vectordb/                #   ChromaDB 持久化
│   │
│   ├── docs/                        # 设计文档
│   │   └── 面试准备-项目优点与可能问题.md
│   │
│   ├── tests/                       # 单元测试
│   └── scripts/                     # 一次性脚本（知识导入/图测试）
│
├── LifeOps助手/                     # ← uni-app 前端
│   ├── pages/
│   │   ├── chat/chat.vue            #   主对话页（消息列表 + 流式渲染）
│   │   ├── bill/bill.vue            #   账单中心页
│   │   └── docs/docs.vue            #   文档中心页
│   ├── components/
│   │   ├── MessageBubble.vue        #   消息气泡（Markdown 渲染）
│   │   ├── InputBar.vue             #   输入框
│   │   └── EmbeddedMap.vue          #   嵌入式地图
│   ├── store/chat.js                #   Pinia 状态管理（WebSocket + 消息流）
│   ├── utils/
│   │   ├── websocket.js             #   WebSocket 客户端（自动重连）
│   │   └── api.js                   #   REST API 客户端
│   └── static/                      #   图标资源
│
└── 项目全貌.md                       # ← 你正在看的这份文档
```

---

## 三、功能模块一览

### 3.1 智能对话（主 Agent）

| 能力 | 说明 |
|------|------|
| 多轮 ReAct 循环 | 主 Agent 每轮全工具可用，LLM 自己判断"继续调工具"还是"回复用户" |
| 四种场景自适应 | 纯闲聊 / 账单分析 / 出行推荐 / 综合查询，一套 Prompt 对号入座 |
| 逐步推进模式 | "每次只调一个工具，看到结果再决定下一步"，避免无效并行调用 |
| 子 Agent 调度 | 自动判断是否需要调用 Bill Agent 或 Travel Agent |

### 3.2 账单分析（Bill Agent）

| 能力 | 说明 |
|------|------|
| 自动下载账单 | IMAP 连接邮箱 → 检测微信账单邮件 → 自动下载 ZIP |
| 自动解析入库 | 解压 → CSV 清洗 → pandas 规范化 → 按月分片存储 |
| 灵活查询 | 任意日期范围的收支明细 + 统计（总支出/总收入/日均/笔数） |
| 数据盘存 | 查看本地有哪些月份的账单数据 |
| 日均消费基线 | 自动计算每日平均支出，用于预算判断 |
| 图表生成 | 月度趋势图 / 分类饼图 |
| 大结果集硬控 | 超过 100 条记录不返回明细，只返回统计 |

### 3.3 出行推荐（Travel Agent）

| 能力 | 说明 |
|------|------|
| POI 搜索 | 百度地图搜索周边餐饮/景点/酒店 |
| 地点详情 | 获取评分/价格/营业时间/图片 |
| 路线规划 | 驾车/公交/步行三种出行方式 |
| 天气查询 | 指定城市的实时天气 |
| 地理编码 | 地址 ↔ 经纬度互转 |
| MCP 双通道容灾 | 百度地图 MCP Server 优先，失败自动 fallback 到原生 @tool |
| 后端路线兜底 | Agent 没调路线工具但用户问了 → 后端自动调百度 Directions API |
| RAG 美食知识库 | 90+ 条中山餐厅实测记录，Chroma + DashScope 向量检索 |
| 预算感知推荐 | 结合账单数据判断预算档位（healthy/tight/critical），影响推荐策略 |

### 3.4 省钱管理

| 能力 | 说明 |
|------|------|
| 设定目标 | 设置省钱目标金额 |
| 更新进度 | 记录已存金额 |
| 财务概览 | 查看目标/进度/预算状态/日均基线 |

### 3.5 安全护栏

| 护栏 | 说明 |
|------|------|
| 计算器强制使用 | 禁止 LLM 自己做任何数学运算，必须调 `calculate` 工具 |
| 工具调用审查 | 最多 8 次总调用 + 同一工具同一参数最多 2 次 |
| 输出审查 | 100+ 禁止话术正则匹配（"其余省略""为保护隐私"等） |
| 正则聚光灯 | 用户输入中所有阿拉伯数字提取为对照表，LLM 提取的金额必须能对上 |
| "搬运工不是翻译器" | 工具预计算统计量，LLM 只嵌入不翻译 |

### 3.6 工程化配套

| 能力 | 说明 |
|------|------|
| 流式输出 | WebSocket 逐字推送 Agent 回复 |
| 地图数据分离传输 | 路线/POI JSON 单独通道推送前端 BMapGL 渲染 |
| 多用户隔离 | SQLite checkpoint 按 thread_id 隔离 |
| 对话持久化 | 断开重连后自动恢复上下文 |
| 工具调用追踪 | TracedToolNode 记录每次调用的参数/返回值/耗时 |
| RAG 监控 | JSONL 埋点 + 控制台仪表盘 + 批量评测 |
| LangSmith 集成 | 全链路追踪 |

---

## 四、技术架构

### 4.1 整体数据流

```
用户输入（WebSocket / CLI）
    │
    ▼
┌─────────────────────────────────────────┐
│           graph_builder.py              │
│         主 Agent ReAct 循环              │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │   MAIN_AGENT_PROMPT_V3         │    │
│  │   (task_decomposer.py)          │    │
│  │   + 正则聚光灯数字对照表          │    │
│  │   + financial_context 注入       │    │
│  └─────────────┬───────────────────┘    │
│                │                         │
│     ┌──────────▼──────────┐             │
│     │   LLM 决策          │             │
│     │   调哪个工具？        │             │
│     │   还是回复用户？      │             │
│     └──┬──────────────┬───┘             │
│        │ 调工具        │ 回复            │
│        ▼               ▼                 │
│  ┌──────────┐    ┌──────────┐           │
│  │ 工具节点  │    │   END    │           │
│  │          │    └──────────┘           │
│  │ 可选工具: │                           │
│  │ query_bill_agent    │                │
│  │ query_travel_agent  │                │
│  │ savings 工具        │                │
│  │ calculate           │                │
│  │ get_current_time    │                │
│  └────┬─────┘                           │
│       │ 调子 Agent 时                     │
│       ▼                                 │
│  ┌──────────────────────┐               │
│  │  Bill Agent /        │               │
│  │  Travel Agent 子图    │               │
│  │  (沙箱隔离，独立 ReAct) │               │
│  └──────────────────────┘               │
└─────────────────────────────────────────┘
    │
    ▼
回复文本 → WebSocket 流式推送前端
地图数据 → 单独通道推送 BMapGL 渲染
```

### 4.2 关键技术选型

| 层 | 选型 | 理由 |
|----|------|------|
| Agent 框架 | LangGraph StateGraph | 手写节点/边/条件路由，不依赖高层封装 |
| LLM | 通义千问 qwen-max | 中文场景表现好，`enable_thinking` 可看推理过程 |
| 向量数据库 | ChromaDB | 本地持久化，个人规模够用，零配置 |
| 向量模型 | DashScope text-embedding-v2 | 1536 维，中文语义检索 |
| 地图服务 | 百度地图 MCP Server | 标准协议，进程级解耦，6 个工具开箱即用 |
| 前端 | uni-app (Vue 3 + Pinia) | 一套代码多端运行（H5 / 微信小程序 / App） |
| 通信 | FastAPI + WebSocket | 流式推送 + REST API 双通道 |
| 状态持久化 | LangGraph SqliteSaver | SQLite checkpoint，按 thread_id 多用户隔离 |
| 账单解析 | pandas + openpyxl | CSV/Excel 读取 + 数据清洗 + 按月分片 |

### 4.3 4 层动态 Prompt 系统

```
平时只加载：
  L1 base 层（角色 + 工具清单）
  L2 decision 层（决策流程 + 停止规则）

按运行时开关激活：
  工具返回异常 → 激活 L3 runtime 降级策略块
  跨 Agent 协作 → 激活 L3 runtime 跨 Agent 上下文块
  预算判断需要 → 激活 L3 runtime 预算感知块（healthy/tight/critical）
  工具连续失败 → 激活 L4 failure 逐工具重试策略

无开关命中 → 仅 L1 + L2（日常精简模式，节省 token）
```

---

## 五、核心设计原则

1. **确定性代码做计算，LLM 做语义包装** —— 工具层用 pandas 预计算所有统计量，LLM 只负责用自然语言说出来
2. **"搬运工不是翻译器"** —— 工具返回预格式化表格，LLM 嵌入不翻译，禁止编造数据
3. **子 Agent 沙箱隔离** —— 子 Agent 内部消息历史不污染主 Agent checkpoint，只返回文本结果
4. **用 Prompt 约束替代代码分叉** —— V3 逐步推进模式让 LLM 自己决定节奏，比硬编码 `has_tool_results` 分叉更灵活
5. **跨 Agent 数据共享走装配层** —— 删除子 Agent 互调工具，financial_context 在 Prompt 装配阶段单向注入，物理上消除递归
6. **工具数量克制** —— 账单 1 个读 + 1 个盘存，地图 6 个 MCP + 1 个 RAG，够用就好

---

## 六、快速定位

| 想看什么 | 去看哪个文件 |
|----------|-------------|
| 主 Agent 图怎么搭的 | [graph/graph_builder.py](Allthing/graph/graph_builder.py) |
| 主 Agent 的 System Prompt | [routing/task_decomposer.py](Allthing/routing/task_decomposer.py) → `MAIN_AGENT_PROMPT_V3` |
| Bill Agent 沙箱长什么样 | [graph/bill_node.py](Allthing/graph/bill_node.py) |
| Travel Agent 沙箱长什么样 | [graph/travel_node.py](Allthing/graph/travel_node.py) |
| Prompt 怎么按需拼装 | [prompts/assembler.py](Allthing/prompts/assembler.py) |
| 账单工具有哪些 | [tools/bill/bill_tools.py](Allthing/tools/bill/bill_tools.py) |
| 地图工具怎么双通道 | [tools/maps/baidu_maps_mcp.py](Allthing/tools/maps/baidu_maps_mcp.py) |
| RAG 怎么做的 | [tools/knowledge/knowledge_tools.py](Allthing/tools/knowledge/knowledge_tools.py) |
| 安全护栏有哪些 | [guardrails/critics.py](Allthing/guardrails/critics.py) |
| 数字对照表怎么防幻觉 | [routing/task_decomposer.py](Allthing/routing/task_decomposer.py) → `scan_number_context` |
| WebSocket 服务端 | [server.py](Allthing/server.py) |
| 前端对话页 | [LifeOps助手/pages/chat/chat.vue](LifeOps助手/pages/chat/chat.vue) |
| 面试准备 | [Allthing/docs/面试准备-项目优点与可能问题.md](Allthing/docs/面试准备-项目优点与可能问题.md) |
